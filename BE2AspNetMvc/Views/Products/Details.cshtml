@model BE2AspNetMvc.Models.Product

@{
    ViewData["Title"] = "Details";
}


<div>
    <div class="d-flex">
        <div class="bg-image mt-3">
            <img src="@Html.DisplayFor(model => model.ImageUrl)" class="img-fluid" alt="@Html.DisplayFor(model => model.Name)">
        </div>
        <div class="card-body p-5">
            <h2 class="card-title">@Html.DisplayFor(model => model.Name)</h2>
            <p class="card-text">@Html.DisplayFor(model => model.Description)</p>
            <h5 class="price pt-2 text-danger">@Html.DisplayFor(model => model.Price) SEK</h5>
            <button class="btn btn-success mt-3">
                <i class="fas fa-shopping-cart me-2"></i>
                LÄGG I KUNDVAGNEN
            </button>
        </div>
    </div>
</div>
    

<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    <a asp-action="Index">Back to List</a>
</div>
